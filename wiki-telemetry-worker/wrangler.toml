name = "wiki-telemetry-worker"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# Same D1 database as the game (see the decision to keep one DB, two
# study arms — the migration in migrations/0002_wiki_tracking.sql adds
# the tables/columns this worker writes to).
[[d1_databases]]
binding = "DB"
database_name = "REPLACE_WITH_YOUR_DB_NAME"
database_id = "REPLACE_WITH_YOUR_DB_ID"
